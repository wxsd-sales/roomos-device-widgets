import type { FavouriteDirectSpaces } from './favourite-direct-spaces';
import type { FavouriteGroupSpaces } from './favourite-group-spaces';

export type FavouriteSpaces = {
  direct: FavouriteDirectSpaces;
  group: FavouriteGroupSpaces;
};
